input {
  tcp {
    port => 5044
    codec => json
  }
}

filter {
  # placeholder for parsing
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "logs-%{+YYYY.MM.dd}"
  }
}
