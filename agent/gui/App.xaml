<Application x:Class="AgentGui.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>

        <!-- ── Palette ── -->
        <Color x:Key="ColBg">#07111E</Color>
        <Color x:Key="ColBg2">#0D1B2E</Color>
        <Color x:Key="ColBg3">#1A2940</Color>
        <Color x:Key="ColAccent">#6C5CE7</Color>
        <Color x:Key="ColGreen">#4ADE80</Color>
        <Color x:Key="ColRed">#EF4444</Color>
        <Color x:Key="ColYellow">#FACC15</Color>
        <Color x:Key="ColCyan">#00D4FF</Color>
        <Color x:Key="ColFg">#C8D1DC</Color>
        <Color x:Key="ColFg2">#9AA4B2</Color>

        <SolidColorBrush x:Key="BrBg"     Color="{StaticResource ColBg}"/>
        <SolidColorBrush x:Key="BrBg2"    Color="{StaticResource ColBg2}"/>
        <SolidColorBrush x:Key="BrBg3"    Color="{StaticResource ColBg3}"/>
        <SolidColorBrush x:Key="BrAccent" Color="{StaticResource ColAccent}"/>
        <SolidColorBrush x:Key="BrGreen"  Color="{StaticResource ColGreen}"/>
        <SolidColorBrush x:Key="BrRed"    Color="{StaticResource ColRed}"/>
        <SolidColorBrush x:Key="BrYellow" Color="{StaticResource ColYellow}"/>
        <SolidColorBrush x:Key="BrCyan"   Color="{StaticResource ColCyan}"/>
        <SolidColorBrush x:Key="BrFg"     Color="{StaticResource ColFg}"/>
        <SolidColorBrush x:Key="BrFg2"    Color="{StaticResource ColFg2}"/>

        <!-- ── Button style ── -->
        <Style x:Key="DarkButton" TargetType="Button">
            <Setter Property="Foreground"        Value="{StaticResource BrFg}"/>
            <Setter Property="Background"        Value="{StaticResource BrBg3}"/>
            <Setter Property="BorderThickness"   Value="0"/>
            <Setter Property="Padding"           Value="18,8"/>
            <Setter Property="FontFamily"        Value="Segoe UI"/>
            <Setter Property="FontSize"          Value="13"/>
            <Setter Property="Cursor"            Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.35"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── TextBox style ── -->
        <Style x:Key="DarkTextBox" TargetType="TextBox">
            <Setter Property="Background"      Value="{StaticResource BrBg3}"/>
            <Setter Property="Foreground"      Value="{StaticResource BrFg}"/>
            <Setter Property="CaretBrush"      Value="{StaticResource BrFg}"/>
            <Setter Property="BorderBrush"     Value="{StaticResource BrBg3}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="8,5"/>
            <Setter Property="FontFamily"      Value="Consolas"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── ProgressBar style ── -->
        <Style x:Key="MetricBar" TargetType="ProgressBar">
            <Setter Property="Height"          Value="7"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background"      Value="{StaticResource BrBg3}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border CornerRadius="3"
                                Background="{TemplateBinding Background}">
                            <Border x:Name="PART_Track">
                                <Border x:Name="PART_Indicator"
                                        HorizontalAlignment="Left"
                                        Background="{TemplateBinding Foreground}"
                                        CornerRadius="3"/>
                            </Border>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── TabControl style ── -->
        <Style TargetType="TabControl">
            <Setter Property="Background"    Value="{StaticResource BrBg}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"       Value="0"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Foreground"    Value="{StaticResource BrFg2}"/>
            <Setter Property="Background"    Value="{StaticResource BrBg2}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"       Value="16,9"/>
            <Setter Property="FontFamily"    Value="Segoe UI"/>
            <Setter Property="FontSize"      Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              TextBlock.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Root" Property="Background"
                                        Value="{StaticResource BrBg3}"/>
                                <Setter Property="Foreground"
                                        Value="{StaticResource BrFg}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground"
                                        Value="{StaticResource BrFg}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ── ScrollBar style (slim dark) ── -->
        <Style TargetType="ScrollBar">
            <Setter Property="Width"       Value="6"/>
            <Setter Property="Background"  Value="{StaticResource BrBg2}"/>
        </Style>

    </Application.Resources>
</Application>
